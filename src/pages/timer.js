import CountdownTimer from "@/components/CountDown";
import React from "react";

const Timer = () => {
  return (
    <div>
      <CountdownTimer />
    </div>
  );
};

export default Timer;
